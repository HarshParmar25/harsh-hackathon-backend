export enum UserRole {
  ADMIN = "admin",
  TEAM_MEMBER = "team-member",
  TEAM_LEAD = "team-lead",
}

export enum ActivationStatus {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
  SUSPENDED = "suspended",
}
